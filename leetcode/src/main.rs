use leetcode::question::q3474::generate_string;

fn main() {
    // Input: str1 = "TFTF", str2 = "ab"
    // Output: "ababa"
    println!("{:?}", generate_string("TFTF".to_string(), "ab".to_string()));
}
